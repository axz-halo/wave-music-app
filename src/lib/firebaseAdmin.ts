// Firebase Admin is optional. To avoid install on builds where it's not used,
// provide empty fallbacks when the module isn't installed.
let adminApp: any = null;
let adminDb: any = null;
let adminAuth: any = null;

export { adminApp, adminDb, adminAuth };


